import { Title, Text, Stack } from '@mantine/core';

export default function SearchAPLPage() {
  return (
    <Stack>
      <Title order={2}>Search APL Process</Title>
      <Text c="dimmed">
        Page shell placeholder. Filter + Result grid land in POC-3 (Steps 16–22).
      </Text>
    </Stack>
  );
}
